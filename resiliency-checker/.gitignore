# Node
node_modules/
npm-debug.log*
package-lock.json

# User-specific runtime state — never commit
data/config.json
data/exclusions.json
data/*.json
!data/.gitkeep

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp

# Local overrides — some users may want to keep a .env alongside
.env
.env.local
